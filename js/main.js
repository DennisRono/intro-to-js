// Variables
/**  var, let, or const */

// Data Types
/** Number - numerical value */
let number = 42
/** string - represents any data inside quotation marks "" */
let name = 'kibet'
/** Boolean - yes/no; 0/1 - true/false data type */
let isHappy = true
/** Null - Empty value */
let nullEmptyValue = null
/** Undefined -  A variable that has been declared but not yet assigned a value.*/
let unassignedValue

/** Object  - Key value pair value (blue print info) */
let person = {
  firstName: 'Dennis',
  lastName: 'Kibet',
  age: 21,
}

/** Array - data type for storing lists*/
let fruits = ['Apple', 'Banana', 'Cherry']

/** Symbol (ES6) - Represents a unique and immutable value that can be used as the key of an object property. */
let symbol1 = Symbol('description')

/** BigInt (ES2020) - Allows you to represent whole numbers larger than 2^53 - 1, which is the largest number JavaScript can reliably represent with the Number type. */
let bigIntNumber = BigInt(1234567890123456789012345678901234567890)
let anotherBigInt = 1234567890123456789012345678901234567890n // Using the 'n' notation

/**
 * Primitive Data Types - that only represent a single value -  is a basic, immutable data type. it hold the actual values and not reference
 */

// Operators
// Functions
// Conditional Statements
// Loops
// Arrays and Objects
// Events
// High Order Functions
/** .map() */
;[1, 2, 3, 4, 5].map((i) => {
  return i * 2
})
/** .filter() */
/** .reduce() */
/** .forEach() */
/** .find() */
/** .some() */
/** .every() */
/** .sort() */
/** .concat() */
/** .flatMap() */
